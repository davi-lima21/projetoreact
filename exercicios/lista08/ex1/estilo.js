import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      alignItems: "center",
    },
    titulo: {
      fontSize: 25,
      color: 'blue'
    }
    
  });

  export default styles;